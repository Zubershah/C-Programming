/*
Write a C program to calculate area of a rectangle Using hard coded inputs.
*/
#include <stdio.h>
int main()
{
    float len = 3;
    float breadth = 6;
    printf("The area of rectangle is %f", len * breadth);
    return 0;
}