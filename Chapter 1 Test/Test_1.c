/*
Write a C program to calculate area of a rectangle Using hard coded inputs.
*/
#include <stdio.h>
int main()
{
    int len = 3;
    int breadth = 6;
    printf("The area of rectangle is %d", len * breadth);
    return 0;
}