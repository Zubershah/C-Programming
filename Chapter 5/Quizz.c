/*
Quick Quiz: Use the library function to calculate the area of a square with side a.
*/

#include <stdio.h>
#include <math.h>
int main()
{
    // area of a square = a^2
    int a = 5;
    printf("the square of the area is %f\n", pow(a, 2));
    return 0;
}