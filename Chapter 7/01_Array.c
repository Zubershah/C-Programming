#include <stdio.h>
int main()
{
    int marks[90];  // index is from 0 to 89; total 90 integar value;
    marks[0] = 45;
    marks[1] = 95; 
    
    printf("marks 0 and marks 1 is  %d %d\n", marks[0], marks[1]);
    return 0;
}